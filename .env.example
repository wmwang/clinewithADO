# =============================================================================
# Cline + Azure DevOps MCP — Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values.
# Never commit .env to version control.
# =============================================================================

# ── Docker image settings ────────────────────────────────────────────────────
DOCKER_IMAGE=your-org/cline-ado
IMAGE_TAG=latest

# ── AI Provider (pick one) ───────────────────────────────────────────────────

# Option A: Anthropic Claude (recommended)
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Option B: OpenAI
# OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Option C: OpenRouter
# OPENROUTER_API_KEY=sk-or-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Option D: OpenAI-compatible with custom endpoint (Azure OpenAI / Ollama / LM Studio / vLLM)
# OPENAI_API_KEY=xxxxxxxxxxxxxxxx
# OPENAI_BASE_URL=https://<resource>.openai.azure.com/openai/deployments/<deployment>
# CLINE_MODEL=gpt-4o
#
# Ollama (local):  OPENAI_BASE_URL=http://host.docker.internal:11434/v1
# LM Studio:       OPENAI_BASE_URL=http://host.docker.internal:1234/v1

# Option E: Fully custom (any provider id that Cline supports)
# CLINE_PROVIDER=openai-compatible
# CLINE_API_KEY=xxxxxxxxxxxxxxxx
# CLINE_MODEL=your-model-name
# CLINE_BASE_URL=https://your-endpoint/v1

# Model override — applies to Option A/B/C if you want a non-default model
# CLINE_MODEL=claude-opus-4-6

# ── Azure DevOps MCP ─────────────────────────────────────────────────────────

# Your Azure DevOps organization name (the part after dev.azure.com/)
# e.g. if your org URL is https://dev.azure.com/contoso → set contoso
ADO_ORG=your-organization-name

# Personal Access Token for headless Docker authentication.
# Required scopes: Code (Read), Work Items (Read & Write), Build (Read)
# Generate at: https://dev.azure.com/<org>/_usersSettings/tokens
ADO_MCP_AUTH_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional: Azure tenant ID (needed for multi-tenant or guest accounts)
# ADO_TENANT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Optional: restrict which ADO tool groups are loaded (comma-separated)
# Available: core, work, work-items, search, test-plans,
#            repositories, wiki, pipelines, advanced-security
# Leave unset to load all groups.
# ADO_MCP_DOMAINS=repositories,work-items,pipelines

# Set to "true" to start Cline without the Azure DevOps MCP server
# ADO_MCP_DISABLED=false
